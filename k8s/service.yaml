apiVersion: v1
kind: Service
metadata:
  name: event-generator-service
  labels:
    app: event-generator
spec:
  selector:
    app: event-generator
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
