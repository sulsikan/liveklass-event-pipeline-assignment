# **과제 — 간단한 이벤트 로그 파이프라인 만들기**

**유형:** 데이터 파이프라인 / 인프라

| **항목** | **내용** |
| --- | --- |
| 제출 기한 | 과제 수령 후 3일 이내 (평일 기준) |
| 제출 방법 | GitHub 레포지터리 링크 + README.md 포함 |

---

## **과제 배경**

웹 서비스에서는 유저의 행동(클릭, 구매, 에러 등)을 이벤트 로그로 기록하고, 이를 분석해 서비스를 개선합니다.

이 과제에서는 **이벤트를 생성하고 → 저장하고 → 분석하고 → 시각화하는** 작은 파이프라인을 직접 만들어 보세요.

---

## **필수 과제**

### **Step 1. 이벤트 생성기 작성**

**Python, Go, Java 중 편한 언어를 선택**하여 웹 서비스에서 발생할 수 있는 이벤트를 랜덤하게 생성하는 스크립트를 작성하세요.

- 이벤트 타입은 2가지 이상 포함하세요 (예: 페이지 조회, 구매, 에러 등)
- 구현 방식, 필드 구성, 생성 건수는 자유롭게 결정하세요
- README에 어떤 이벤트를 왜 그렇게 설계했는지 간략히 설명하세요

### **Step 2. 로그 저장**

생성한 이벤트를 **본인이 적합하다고 생각하는 저장소에 자유롭게 저장**하세요. (파일 기반 또는 데이터베이스)

**요구사항**

- 단순히 JSON을 통째로 저장하지 말고, 필드를 구분하여 저장하세요
- 사용한 저장소의 스키마 또는 데이터 구조를 README에 포함하세요
- README에 해당 저장소를 선택한 이유를 반드시 작성하세요

### **Step 3. 데이터 집계 분석**

저장된 데이터를 분석하는 쿼리(또는 코드)를 **최소 2개** 작성하세요. 아래 예시 중 골라도 되고, 직접 생각한 분석을 해도 됩니다.

- 이벤트 타입별 발생 횟수
- 유저별 총 이벤트 수
- 시간대별 이벤트 추이
- 에러 이벤트 비율

### **Step 4. Docker로 실행 가능하게 만들기**

**`docker-compose.yml`**을 작성해서 **`docker compose up`** 한 번으로 전체 스택이 실행되게 구성하세요.

- 앱 + DB를 함께 구성하세요
- 실행 후 이벤트 생성 → 저장까지 자동으로 동작해야 합니다

### **Step 5. 결과 시각화**

SQL 집계 결과를 시각화하세요. 아래 중 편한 방법을 선택하면 됩니다.

- 차트 생성 후 이미지 파일로 저장
- 다른 BI 도구로 대시보드 구성 후 스크린샷 제출

## **선택 과제**

> ***필수 과제를 완료한 후 여유가 있다면 도전해 보세요. 둘 중 하나만 해도 됩니다.***
> 

### **선택 A. Kubernetes 기초 이해**

아래 두 가지를 작성하세요.

**1) Kubernetes 리소스 설정 파일 작성** Step 1에서 만든 이벤트 생성기 앱을 Kubernetes에 배포한다고 가정하고, **2가지 이상**의 manifest 파일을 작성하세요. 실제로 클러스터에 배포하지 않아도 됩니다.

**2) README에 아래 내용 서술**

- 선택한 Kubernetes 리소스의 역할 설명
- Kubernetes 리소스를 선택한 이유

### **선택 B. AWS 기초 이해**

아래 두 가지를 작성하세요.

**1) AWS 아키텍처 설계** 이 과제의 파이프라인(이벤트 생성 → 저장 → 시각화)을 AWS에서 운영한다면 어떤 서비스를 사용할지 구성도를 그리세요. draw.io, Excalidraw, 손으로 그린 사진 모두 가능합니다.

- 사용할 AWS 서비스를 명시하고 (예: EC2, RDS, S3, CloudWatch 등)
- 각 서비스를 선택한 이유를 간략히 설명하세요

**2) README에 아래 내용 서술**

- 선택한 AWS 서비스의 역할 차이를 본인의 말로 설명
- 설계한 아키텍처에서 가장 고민한 부분

---

## **README 필수 항목**

아래 3가지를 README에 반드시 포함하세요. 분량은 짧아도 됩니다.

**1. 실행 방법**

```
필요한 도구, 설치 명령어, 실행 명령어를 순서대로 작성
```

**2. 스키마 설명**

```
테이블을 이렇게 설계한 이유를 2~3줄로 설명
```

**3. 구현하면서 고민한 점**
```
어떤 결정을 했는지, 왜 그렇게 했는지 자유롭게 작성
(잘 모르는 부분이 있었다면 어떻게 해결했는지도 좋습니다)
```